# Linting and Formatting Rules

## Linting and Formatting

- Resolve all ESLint errors and warnings.
- Keep code formatted with Prettier.
- Resolve lint violations before committing.
- Use ESLint disable comments only when strictly necessary and include a reason comment.
- Keep Stylelint clean for stylesheet files (`.css`, `.scss`, `.sass`, `.less`).
