extends /components/templates/list/index.pug

block prepend heroContent
  -
    const primaryHero = {
      path: null,
    };

block prepend mainContent
  -
    const headingProp = {
      level: 1,
      visible: true,
    };
    const headingTitle = page.category;
    const posts = page.posts.data || page.posts || [];

block sidebar