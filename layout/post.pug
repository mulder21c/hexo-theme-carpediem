extends /components/templates/post/index

block prepend heroContent
  -
    const primaryHero = {
      path: page.hero,
    };

block prepend mainContent
  -
    const post = page;
    const author = {
      name: config.author,
      gravatar: theme.profile.gravatar,
      description: theme.profile.description,
    };