//- @pugdoc
  name: remark
  description: remark comments component
  beforeEach: |
    include /components/utils/util
  arguments:
    - {object} [props] [`{}`]
  examples:
    - |
      +remark()

mixin remark(props= {})
  -
    // define types
    const remarkProps = {};

    // verifying props
    validateProps(remarkProps)(props);

  div#remark42()&attributes(props)
