/**
 * Jest mock for CSS/SCSS modules. Components use classNames.bind(styles);
 * an empty object allows class names to be applied as raw strings.
 */
// eslint-disable-next-line no-undef -- CommonJS export for Jest
module.exports = {};
